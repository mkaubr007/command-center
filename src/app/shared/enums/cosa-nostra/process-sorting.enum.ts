export enum Sorting {
    ASC = 'asc',
    DESC = 'desc',
  }
  
  export enum OrderBy {
    ASC = 1,
    DESC = -1,
  }
  
  export enum SortBy {
    NAME = 'name',
    DISPLAY_NAME = 'displayName',
    ASSEMBLY = 'assembly',
  }